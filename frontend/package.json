{
  "name": "latest-trailers-frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "s3-deploy": "^0.7.3"
  },
  "scripts": {
    "deployPreprod": "s3-deploy '{index.html,assets/**}' --region ap-southeast-2 --bucket latesttrailerstest",
    "deploy": "s3-deploy '{index.html,assets/**}' --region us-east-2 --bucket latesttrailers.net"
  }
}
